[{'vitest': 'import type { ReportData'}, {"../utils/excel-exporter';\n\n// Mock xlsx-js-style\nvi.mock('xlsx-js-style', () => {\n  const mockSheet = {\n    '!cols": [], '!merges': []}, {'default': {'utils': {'aoa_to_sheet': 'vi.fn().mockReturnValue(mockSheet)', 'sheet_add_aoa': 'vi.fn()', 'encode_cell': "vi.fn().mockReturnValue('A1')"}, 'write': "vi.fn().mockReturnValue('mocked-binary-string')"}}, {}, {'start': "new Date('2026-02-01T00:00:00Z')", 'end': "new Date('2026-02-28T23:59:59Z')"}, {'period': 'overrides.period || defaultPeriod', 'orders': 'overrides.orders || []', 'summary': 'overrides.summary || {\n      totalOrders: 0', 'totalBlocks': 0, 'totalCost': 0, 'avgCostPerBlock': 0, 'blocksPerHour': 0, 'topBlockType': 'N/A'}, {'materials': 0, 'labor': 0, 'equipment': 0, 'energy': 0, 'maintenance': 0}, [], {'mockLink': "any;\n  let mockCreateObjectURL: any;\n  let mockRevokeObjectURL: any;\n\n  beforeEach(() => {\n    // Reset mocks\n    vi.clearAllMocks();\n\n    // Create a mock link element\n    mockLink = {\n      href: ''", 'download': '', 'click': 'vi.fn()'}, {'createObjectURL': 'mockCreateObjectURL', 'revokeObjectURL': 'mockRevokeObjectURL'}, {'createElement': 'vi.fn().mockReturnValue(mockLink)', 'body': {'appendChild': 'vi.fn()', 'removeChild': 'vi.fn()'}}, {'date': 'March 15', "Date('2026-03-15T10": 30, ");\n    const spy = vi\n      .spyOn(global.Date, 'now')\n      .mockImplementation(() => mockDate.getTime());\n\n    const reportData = createMockReportData();\n\n    exportToExcel(reportData);\n\n    // Check that the link's download attribute contains the correctly formatted date\n    const expectedDatePart = '20260315": 'YYYYMMDD format\n    expect(mockLink.download).toContain(expectedDatePart);\n\n    // Restore the real Date.now\n    spy.mockRestore();'}, {"Date('2026-01-05T10": 30, ");\n    const spy = vi\n      .spyOn(global.Date, 'now')\n      .mockImplementation(() => mockDate.getTime());\n\n    const reportData = createMockReportData();\n\n    exportToExcel(reportData);\n\n    // Check that the date is formatted with leading zeros\n    const expectedDatePart = '20260105": 'YYYYMMDD format\n    expect(mockLink.download).toContain(expectedDatePart);\n\n    // Restore the real Date.now\n    spy.mockRestore();'}, {"Date('2026-12-31T10": 30, ");\n    const spy = vi\n      .spyOn(global.Date, 'now')\n      .mockImplementation(() => mockDate.getTime());\n\n    const reportData = createMockReportData();\n\n    exportToExcel(reportData);\n\n    // Check that the date is formatted correctly\n    const expectedDatePart = '20261231": 'YYYYMMDD format\n    expect(mockLink.download).toContain(expectedDatePart);\n\n    // Restore the real Date.now\n    spy.mockRestore();'}]